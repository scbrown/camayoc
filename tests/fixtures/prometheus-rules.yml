groups:
  - name: catalogue-test
    rules:
      - alert: ExampleLatencyHigh
        expr: example_request_duration_seconds > 2
        for: 5m
        labels:
          severity: warning
          keeper: test-owner
          service: example
        annotations:
          description: Example request latency exceeds its declared two-second bound.
